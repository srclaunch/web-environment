import { DateTime } from 'luxon';
import { LoginEvent, LogoutEvent, SignupEvent } from '../applab/analytics/user/authentication';
import { PageViewEvent, WebsiteVisitEvent } from '../applab/analytics/web';
export declare type PersonalAnalyticsDetails = {
    authentication: {
        history: (LoginEvent | LogoutEvent | SignupEvent)[];
        logins: {
            first_login: DateTime;
            last_login: DateTime;
            count: number;
        };
        signed_up?: boolean;
    };
    value: {
        lifetime: number;
    };
    website: {
        pages: {
            views: {
                count: number;
                history: PageViewEvent[];
            };
        };
        visits: {
            count: number;
            history: WebsiteVisitEvent[];
        };
    };
};
//# sourceMappingURL=analytics.d.ts.map